//Write a thats uses a for loop to print a multiplication table for a specified number

public class MultiplicationTable {
    public static void main(String[] args) {
        int number = 9; // Change the number for a different multiplication table

        System.out.println("Multiplication table for " + number + ":");
        for (int i = 1; i <= 10; i++) {
            System.out.println(number + " x " + i + " = " + (number * i));
        }
    }
}
